name: dock

# Pure docking reward: dock score only, no QED, no SA.
# Matches RGFN (DRD2/sEH proxy) and HN-GFN (GSK3β proxy) baselines.

# Target protein — directory name under Data/docking_targets/
# Available: seh (sEH, PDB 1ZD5), drd2 (DRD2, PDB 3PBL), gsk3b (GSK3β, PDB 1UV5)
target: seh

# Pure dock score
dock_weight: 1.0
sa_weight: 0.0
