# Python
__pycache__/
*.pyc

# C++ extension build artifacts
csrc/build*/
csrc/cenv.so
*.sif

# Confidential data — never commit (symlink to private dataset)
Data/anti_400.txt

# Run outputs: contain generated molecules (confidential if from private data).
# Re-added per run; never tracked. Keep the dir via .gitkeep.
Experiments/
!Experiments/.gitkeep

# Design docs kept local, out of history
docs/

# Temp / rendezvous / logs
tem/
tem_*/
log/
*.log

# Stray dirs
rewards/
results/

# External reference repos (cloned locally, not part of this repo)
refs/
